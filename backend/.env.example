PORT=3001
NODE_ENV=development
# IMPORTANTE: En producción usa un JWT_SECRET fuerte y único (ej: openssl rand -base64 32)
JWT_SECRET=super-secret
ADMIN_DEFAULT_PASSWORD=Admin123!
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
SMTP_FROM=
APP_URL=http://localhost:5173
CORS_ORIGINS=http://localhost:5173
TRUST_PROXY=0
LOGIN_RATE_WINDOW_MS=900000
LOGIN_RATE_MAX=10
HEADER_LOGO_PATH=
WATERMARK_LOGO_PATH=
CLIENTES_CSV_PATH=
# Carpeta donde se guardan los PDFs (vacío = remisiones-pdf dentro del proyecto)
PDF_OUTPUT_DIR=
