{
  "name": "epsi-hl-iris",
  "private": true,
  "scripts": {
    "test": "npm run test --prefix backend && npm run test --prefix frontend/vite-project",
    "test:coverage": "npm run test:coverage --prefix backend && npm run test:coverage --prefix frontend/vite-project"
  }
}
